Автор: Маслов Никита Сергеевич
Группа 521
Вариант 1

Для сборки проекта используется cmake. Используется стандарт C++11 (поддерживается современными g++).
Проверено на сборку с GCC версии 5.4.1.

Для парсинга XML-файла использована библиотека TinyXML2 (https://github.com/leethomason/tinyxml2), 
которая прилагается в виде исходного кода и не требует дополнительной установки.

Порядок сборки:

  $ mkdir build
  $ cd build
  $ cmake ..
  $ make

Для проверки работы можно использовать приложенный скрипт run_tests.sh. Использование:

  $ ./run_tests.sh ./build/prog_02170392_521 /path/to/tests/dir/

где /path/to/tests/dir - путь до директории с тестовыми данными для варианта 1. Директория
должна содержать поддиректории YES и NO с соответствующими входными файлами.

По результатам проверки скрипт выведет сообщения для проблемных файлов и вердикт
всего тестирования (PASS или FAIL).
